<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@android:color/darker_gray"> <!-- The color of the ripple effect -->
    <item android:id="@android:id/mask">
        <shape
            android:shape="rectangle">
            <solid android:color="@android:color/white"/> <!-- Button's default background color -->
        </shape>
    </item>
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="@android:color/white"/> <!-- Button's default background color -->
        </shape>
    </item>
</ripple>
